# 🏗️ Architecture Overview - Cross-Device Session Sync

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         SMART ATTEND APP                             │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────────┐                      ┌──────────────────────┐
│   FACULTY DEVICE     │                      │   STUDENT DEVICE     │
│                      │                      │                      │
│  ┌────────────────┐  │                      │  ┌────────────────┐  │
│  │ Create Session │  │                      │  │  Join Session  │  │
│  │     Page       │  │                      │  │      Page      │  │
│  └────────┬───────┘  │                      │  └────────┬───────┘  │
│           │          │                      │           │          │
│           ▼          │                      │           ▼          │
│  ┌────────────────┐  │                      │  ┌────────────────┐  │
│  │ DemoDataService│  │                      │  │ DemoDataService│  │
│  │ .createSession │  │                      │  │ .getActiveSess │  │
│  │                │  │                      │  │   ionsStream() │  │
│  └────────┬───────┘  │                      │  └────────┬───────┘  │
│           │          │                      │           │          │
│           ▼          │                      │           ▼          │
│  ┌────────────────┐  │                      │  ┌────────────────┐  │
│  │Firebase Session│  │                      │  │Firebase Session│  │
│  │    Service     │  │                      │  │    Service     │  │
│  └────────┬───────┘  │                      │  └────────┬───────┘  │
│           │          │                      │           │          │
└───────────┼──────────┘                      └───────────┼──────────┘
            │                                             │
            │                                             │
            ▼                                             ▼
    ┌───────────────────────────────────────────────────────────┐
    │                  FIREBASE FIRESTORE                        │
    │                                                            │
    │  Collection: sessions                                      │
    │  ┌──────────────────────────────────────────────────────┐ │
    │  │ Document: session_123456789                          │ │
    │  │ {                                                     │ │
    │  │   id: "123456789",                                    │ │
    │  │   courseId: "SUB001",                                 │ │
    │  │   courseName: "CS201 - Data Structures",             │ │
    │  │   courseCode: "CS201",                                │ │
    │  │   facultyId: "2001",                                  │ │
    │  │   facultyName: "Dr. Suresh Reddy",                    │ │
    │  │   latitude: 12.9716,                                  │ │
    │  │   longitude: 77.5946,                                 │ │
    │  │   radius: 50,                                         │ │
    │  │   createdAt: "2025-12-09T10:30:00.000Z",              │ │
    │  │   expiresAt: "2025-12-09T10:32:00.000Z",              │ │
    │  │   isActive: true                                      │ │
    │  │ }                                                     │ │
    │  └──────────────────────────────────────────────────────┘ │
    │                                                            │
    │  Real-time Listeners: 2 active                             │
    │  Auto-sync: Enabled                                        │
    └───────────────────────────────────────────────────────────┘
                            ▲
                            │
                    ┌───────┴────────┐
                    │  Real-time     │
                    │  Sync Engine   │
                    │  (Firestore)   │
                    └────────────────┘
```

## Data Flow Diagram

```
FACULTY CREATES SESSION:
═══════════════════════

1. Faculty Opens App
   │
   ▼
2. Selects Course & Radius
   │
   ▼
3. Clicks "Start Session"
   │
   ▼
4. Get Current Location (GPS)
   │
   ▼
5. Create Session Object
   ├─ id: timestamp
   ├─ courseId, courseName, courseCode
   ├─ facultyId, facultyName
   ├─ latitude, longitude, radius
   ├─ createdAt, expiresAt
   └─ isActive: true
   │
   ▼
6. DemoDataService.createSession()
   │
   ▼
7. FirebaseSessionService.createSession()
   │
   ▼
8. Firestore.collection('sessions').doc(id).set()
   │
   ▼
9. ✅ Session stored in cloud
   │
   ▼
10. Real-time sync triggers
    │
    └─────────────────────────────────┐
                                      │
STUDENT SEES SESSION:                 │
═══════════════════                   │
                                      │
1. Student Opens "Join Session"       │
   │                                  │
   ▼                                  │
2. StreamBuilder initialized          │
   │                                  │
   ▼                                  │
3. DemoDataService.getActiveSessionsStream()
   │                                  │
   ▼                                  │
4. FirebaseSessionService.getActiveSessionsStream()
   │                                  │
   ▼                                  │
5. Firestore.snapshots() ◄───────────┘
   │
   ▼
6. Stream emits session data
   │
   ▼
7. StreamBuilder rebuilds UI
   │
   ▼
8. ✅ Session appears on screen!
   │
   ▼
9. Countdown timer starts
   │
   ▼
10. Student clicks "Mark Present"
    │
    ▼
11. Verify location within radius
    │
    ▼
12. ✅ Attendance marked!
```

## Component Interaction

```
┌─────────────────────────────────────────────────────────────┐
│                        UI LAYER                              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  CreateGeoSessionPage          JoinSessionPage              │
│  ┌──────────────────┐          ┌──────────────────┐         │
│  │ - Course Select  │          │ - StreamBuilder  │         │
│  │ - Radius Slider  │          │ - Session List   │         │
│  │ - Start Button   │          │ - Countdown      │         │
│  │ - Timer Display  │          │ - Mark Button    │         │
│  └────────┬─────────┘          └────────┬─────────┘         │
│           │                             │                   │
└───────────┼─────────────────────────────┼───────────────────┘
            │                             │
            ▼                             ▼
┌─────────────────────────────────────────────────────────────┐
│                     SERVICE LAYER                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  DemoDataService                                             │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ + createSession(session)                               │ │
│  │ + getActiveSessions() → Future<List>                   │ │
│  │ + getActiveSessionsStream() → Stream<List>             │ │
│  │ + clearExpiredSessions()                               │ │
│  │                                                         │ │
│  │ Logic:                                                  │ │
│  │ - Check if Firebase available                          │ │
│  │ - Use Firebase if available                            │ │
│  │ - Fallback to SharedPreferences                        │ │
│  └────────┬───────────────────────────────────────────────┘ │
│           │                                                  │
└───────────┼──────────────────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────────────────────────┐
│                   FIREBASE LAYER                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  FirebaseSessionService                                      │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ + createSession(session)                               │ │
│  │ + getActiveSessions() → Future<List>                   │ │
│  │ + getActiveSessionsStream() → Stream<List> ⭐          │ │
│  │ + endSession(id)                                        │ │
│  │ + deleteSession(id)                                     │ │
│  │ + cleanupExpiredSessions()                              │ │
│  │                                                         │ │
│  │ Firestore Queries:                                      │ │
│  │ - where('isActive', ==, true)                          │ │
│  │ - where('createdAt', >, twoMinutesAgo)                 │ │
│  │ - orderBy('createdAt', desc)                           │ │
│  │ - .snapshots() for real-time                           │ │
│  └────────┬───────────────────────────────────────────────┘ │
│           │                                                  │
└───────────┼──────────────────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────────────────────────┐
│                   DATA LAYER                                 │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Firebase Firestore (Cloud)                                  │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ Collection: sessions                                    │ │
│  │ - Real-time sync                                        │ │
│  │ - Automatic indexing                                    │ │
│  │ - Security rules                                        │ │
│  │ - Offline persistence                                   │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  SharedPreferences (Local Fallback)                          │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ Key: active_sessions                                    │ │
│  │ - JSON string list                                      │ │
│  │ - Device-local only                                     │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## Real-Time Sync Mechanism

```
TIME: T0 (Faculty creates session)
════════════════════════════════════

Faculty Device              Firestore Cloud           Student Device
     │                            │                         │
     │  POST /sessions/123        │                         │
     ├───────────────────────────>│                         │
     │                            │                         │
     │  ✅ Created                │                         │
     │<───────────────────────────┤                         │
     │                            │                         │
     │                            │  📡 Snapshot Event      │
     │                            ├────────────────────────>│
     │                            │                         │
     │                            │                         │  ✅ UI Updates
     │                            │                         │  (StreamBuilder)
     │                            │                         │


TIME: T+30s (Session still active)
═══════════════════════════════════

Faculty Device              Firestore Cloud           Student Device
     │                            │                         │
     │                            │  📡 Continuous Stream   │
     │                            │<───────────────────────>│
     │                            │                         │
     │                            │                         │  ⏱️ Countdown: 1m 30s
     │                            │                         │  (Updates every 1s)


TIME: T+2m (Session expires)
════════════════════════════

Faculty Device              Firestore Cloud           Student Device
     │                            │                         │
     │  Timer ends                │                         │
     │  Session inactive          │                         │
     │                            │                         │
     │                            │  Filter: isActive=true  │
     │                            │  createdAt > 2min ago   │
     │                            │                         │
     │                            │  📡 Empty List          │
     │                            ├────────────────────────>│
     │                            │                         │
     │                            │                         │  ✅ Session removed
     │                            │                         │  from UI
```

## State Management

```
┌─────────────────────────────────────────────────────────────┐
│                  STUDENT JOIN SESSION PAGE                   │
└─────────────────────────────────────────────────────────────┘

StreamBuilder States:
═══════════════════════

1. ConnectionState.waiting
   ┌──────────────────────────┐
   │   🔄 Loading Spinner     │
   │   "Connecting..."        │
   └──────────────────────────┘

2. snapshot.hasError
   ┌──────────────────────────┐
   │   ❌ Error Icon          │
   │   "Error loading"        │
   │   [Retry Button]         │
   └──────────────────────────┘

3. snapshot.data.isEmpty
   ┌──────────────────────────┐
   │   📭 Empty Icon          │
   │   "No active sessions"   │
   │   "Waiting for faculty"  │
   │   🔄 Listening...        │
   └──────────────────────────┘

4. snapshot.hasData
   ┌──────────────────────────┐
   │  ┌────────────────────┐  │
   │  │ CS201 - Data Str.. │  │
   │  │ Faculty: Dr. Suresh│  │
   │  │ ⏱️ Expires: 1m 45s │  │
   │  │ [Mark Present]     │  │
   │  └────────────────────┘  │
   │  ┌────────────────────┐  │
   │  │ CS202 - DBMS       │  │
   │  │ Faculty: Prof. Anj │  │
   │  │ ⏱️ Expires: 0m 30s │  │
   │  │ [Mark Present]     │  │
   │  └────────────────────┘  │
   └──────────────────────────┘
```

## Performance Optimization

```
Firestore Query Optimization:
═══════════════════════════════

✅ Indexed Fields:
   - isActive
   - createdAt
   
✅ Composite Index (auto-created):
   - isActive (ASC) + createdAt (DESC)

✅ Query Limits:
   - Only active sessions (isActive = true)
   - Only recent sessions (createdAt > 2 min ago)
   - Ordered by creation time

✅ Real-time Listeners:
   - Efficient snapshot updates
   - Only changed documents transmitted
   - Automatic reconnection

✅ Local Caching:
   - Offline persistence enabled
   - Cached data used when offline
   - Automatic sync when online
```

---

**This architecture provides:**
- ⚡ Real-time updates (< 1 second latency)
- 🔄 Automatic synchronization
- 📱 Cross-device support
- 💾 Offline fallback
- 🛡️ Error resilience
- 📊 Scalable design
